# Changelog

## 1.0.0 — 2026-09

Первый публичный выпуск: node-guard (отвал, load на ядро, память, полоса от ёмкости,
«в маршрутизации, но молчит» по профилям мостов с `parked`/`alt_ips`, цепочки вход → выход),
traffic-guard (сырые счётчики интерфейса, учёт по месяцам с перезагрузками, max|sum, пороги
по прогнозу, одно письмо на порог, `--report`), мастер `fleet-guards setup` (панель, профили,
объём, Telegram с автоопределением получателя, первый отчёт, таймеры), `doctor`, установка
одной командой без затирания чужого, `uninstall.sh`, документы.
